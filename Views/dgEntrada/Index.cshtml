@model IEnumerable<DidierGuerrero_Examen1P.Models.DidierGuerrero__Tabla>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dgEntradasDisponibles)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dgPrecio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dgNombrePelicula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dgFechaPelicula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MdgMayorEdad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dgEntradasDisponibles)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dgPrecio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dgNombrePelicula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dgFechaPelicula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MdgMayorEdad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
